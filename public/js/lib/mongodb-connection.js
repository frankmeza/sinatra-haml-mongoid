// let NAMES_COLLECTION;

// MongoClient.connect('mongodb://localhost:27017/names', (err, connection) => {
//   if (err) return console.log(err);
//   NAMES_COLLECTION = connection.collection('names')
// })
